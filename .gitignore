# Never commit real secrets — only .env.example is tracked.
.env
.env.local
.env.*.local

# Vercel CLI local project/link metadata.
.vercel

# Node dependency output, in case anything is ever installed at the root.
node_modules/

# Confidential source documents — read-only final truth, kept out of version
# control (the sourced programme content lives in tosp/js/programmes/ instead).
Amazon_Team/
PH_Team/
Ebay_Team/
Digital_Marketing_Team/
Purchasing_Team/
